enum NotFound {
  home = 'NotFound.home',
  text = 'NotFound.text',
  title = 'NotFound.title',
}

enum Posts {
  clear = 'Posts.clear',
  textInputLabel = 'Posts.textInputLabel',
  textInputPlaceholder = 'Posts.textInputPlaceholder',
  title = 'Posts.title',
}

enum Navbar {
  home = 'Navbar.home',
}

const TranslationConstants = { Posts, NotFound, Navbar };

export default TranslationConstants;
